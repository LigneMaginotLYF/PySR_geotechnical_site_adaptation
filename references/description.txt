This folder contains examples of PySR output folders (for formula extraction and compilation) and .py files that show: (1) extraction of expressions from text strings BUT with "^" operator still undefined (should be "**"); (2) dynamic or static fine-tuning (albeit with a DIFFERENT LOGIC) based on existing formulae and a PORTION of new site data; (3) Bayesian updating of undetermined parameters (again, different parameters from this new task).
