# PySR_geotechnical_site_adaptation
A geotechnical knowledge transfer framework. PySR is first used to extract expressions from existing (old) datasets. When a portion of a new dataset arrives from a parallel engineering site for adaptation, dictionary learning or candidate filtering is performed and Bayesian inference is used to decide the weights for the remaining expressions.
